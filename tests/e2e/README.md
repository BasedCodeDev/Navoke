# E2E Smoke Tests

This folder is reserved for Electron UI smoke tests. Once the first real selector calibration is added, run:

```powershell
npm run test:e2e
```

The unit test suite already validates workflow registration and artifact generation.
